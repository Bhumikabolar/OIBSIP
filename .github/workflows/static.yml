# Simple workflow for deploying static content to GitHub Pages
name: Deploy NexaAI Landing Page

on:
  push:
    branches: ["main"]

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Task 1 Landing Page
        uses: actions/upload-pages-artifact@v3
        with:
          path: './Web Development-Level 1-Landing Page/Task-1-Landing-Page'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
